[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pandss"
version = "0.6.9"
dependencies = [
    "pyhecdss",
    "pandas",
    "pint"
]

[tool.setuptools]
include-package-data = true