[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pandss"
version = "0.6.6"
dependencies = [
    "pyhecdss",
    "pandas",
]

[tool.setuptools]
include-package-data = true