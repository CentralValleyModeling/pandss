[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pandss"
version = "0.7.1"
dependencies = [
    "pyhecdss",
    "pandas",
    "packaging"
]

[tool.setuptools]
include-package-data = true